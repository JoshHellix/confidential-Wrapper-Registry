{
  "name": "wrapper-registry",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "wrapper-registry/frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w wrapper-registry",
    "build": "npm run build -w wrapper-registry",
    "build:shared": "npm run build -w @zama-season3/shared"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
